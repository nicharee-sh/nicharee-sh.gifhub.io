const animalFacts = (
    <div>
        <title>
      <h1>
      'Click an animal for a fun fact'
      </h1>
      <p>
        {Math.PI.toFixed(20)}
      </p>
      </title>
    </div>
  );
  
  ReactDOM.render(
     animalFacts,
    document.getElementById('app')
  );